# WinNap UI Library v2.3

Modern UI Framework for CustomTkinter Applications

WinNap คือไลบรารี UI ที่ออกแบบมาเพื่อสร้างแอปเดสก์ท็อปสไตล์ modern ได้อย่างรวดเร็ว มีระบบหน้าต่าง, ระบบนำทาง (Sidebar), ระบบธีม, การ์ด, แจ้งเตือน และคอมโพเนนต์พื้นฐานครบชุด

> จุดเด่นเวอร์ชัน 2.3: เริ่มต้นหน้าแรกอัตโนมัติทันที **ไม่มีหน้า Welcome**

---

## คุณสมบัติหลัก (Features)

* ระบบหน้าต่างแบบ Modern (Draggable, Topmost, ไม่มีขอบ)
* ระบบ Sidebar Navigation พร้อมสลับหน้า
* ระบบ Theme (Dark, Light, Purple, Cyberpunk)
* Page System สำหรับจัดโครงสร้างแอป
* Card UI สำหรับจัดกลุ่มข้อมูล
* Notification Popup แบบ Fade In/Out
* รองรับปุ่ม, สวิตช์, สไลเดอร์, ProgressBar, Entry, ComboBox
* ระบบ Status Bar
* Auto Switch หน้าแรกอัตโนมัติ

---

## โครงสร้างคลาสหลัก

* `WinNapTheme`
* `WinNapWindow`
* `WinNapPage`
* `WinNapCard`
* `WinNapNotification`

---

# 1. WinNapTheme

ระบบจัดการธีมสีของแอป

### ฟังก์ชัน

* `get_theme(theme_name)` → ดึงชุดค่าสีของธีม
* `list_themes()` → แสดงรายชื่อธีมทั้งหมด

### ธีมที่มีให้

* dark
* light
* purple
* cyberpunk

---

# 2. WinNapWindow

คลาสหลักสำหรับสร้างหน้าต่างแอปทั้งหมด

### การสร้างหน้าต่าง

```python
app = WinNapWindow(
    title="My App",
    width=1000,
    height=700,
    theme="dark",
    topmost=True,
    resizable=True,
    center=True
)
```

### ฟังก์ชันหลัก

* `add_page(name, title, icon=None)` → เพิ่มหน้าใหม่
* `switch_page(name)` → เปลี่ยนหน้า
* `set_status(text)` → ตั้งค่าข้อความด้านล่าง
* `show_notification(text, duration=3)` → แสดงแจ้งเตือน
* `run()` → เริ่มการทำงานของแอป
* `get_root()` → ดึง root window

✅ หน้าแรกจะถูกเปิดอัตโนมัติจาก `add_page()` ตัวแรก

---

# 3. WinNapPage

ใช้สำหรับจัดวาง UI ของแต่ละหน้า

### ฟังก์ชันสำคัญ

* `add_header(title, subtitle=None)`
* `add_card(title, content=None)`
* `add_section(title, description=None)`
* `add_button(text, command=None)`
* `add_switch(text, command=None, default=False)`
* `add_label(text)`
* `add_entry(placeholder="")`
* `add_slider(from_=0, to=100)`
* `add_progressbar()`
* `add_combobox(values)`
* `clear()` ล้าง UI ทั้งหน้า

---

# 4. WinNapCard

ใช้สำหรับจัดกลุ่มข้อมูลหรือปุ่มภายใน Card

### ฟังก์ชัน

* `add_label(text)`
* `add_button(text, command=None)`
* `add_switch(text, command=None, default=False)`

---

# 5. WinNapNotification

ระบบแจ้งเตือนมุมจอแบบ Fade Animation

### การใช้งาน

```python
app.show_notification("บันทึกข้อมูลสำเร็จ", 3)
```

---

# 6. ตัวอย่างการสร้างแอป (Example App)

```python
app = WinNapWindow(
    title="WinNap Demo",
    width=1100,
    height=750,
    theme="cyberpunk"
)

# หน้าแรก (จะเปิดอัตโนมัติ)
home = app.add_page("home", "Dashboard", "🏠")
home.add_header("Dashboard", "หน้าหลักของระบบ")

card = home.add_card("System Status")
card.add_label("Server: Online")
card.add_label("Users: 120 คน")

home.add_button("แสดงแจ้งเตือน", lambda: app.show_notification("ยินดีต้อนรับเข้าสู่ระบบ"))

# หน้าตั้งค่า
settings = app.add_page("settings", "Settings", "⚙️")
settings.add_header("Settings", "การตั้งค่าระบบ")

settings.add_switch("เปิดโหมดดีบัก")
settings.add_slider(0, 100)
settings.add_combobox(["Low", "Medium", "High"])

# เริ่มแอป
app.run()
```

---

## แนวทางการใช้งานจริง

* เหมาะสำหรับ: Control Panel, Launcher, Tool, Cheat GUI, Admin Panel, Dashboard
* รองรับการขยายระบบ Plugin และ Module เพิ่มได้
* สามารถใช้คู่กับ Thread, API, และระบบไฟล์ได้

---

## เวอร์ชัน

**WinNap UI Library v2.3**

* ตัด Welcome Screen ออก
* หน้าแรกเปิดอัตโนมัติ
* ปรับ Sidebar และ Notification ให้ลื่นขึ้น

---

## License

ใช้ได้ฟรีสำหรับโปรเจกต์ส่วนตัวและเชิงพาณิชย์
